/**
 * <p>Contains classes for reading and writing to files.</p>
 * <p>All these classes are used in the CSV class as helpers. These classes can also be used standalone for
 * where rading very large data sets. Then the data setRaw is not kept in memory but streamed.</p>
 */
package no.laukvik.csv.io;
